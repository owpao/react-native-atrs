import { StyleSheet, Image } from 'react-native';
export const styles = StyleSheet.create({
    bg: {
        flex: 1,
        width:null,
        height:null,
        resizeMode: 'cover'
    },
    container: {
        flex: 1,
        alignItems: 'center',
        justifyContent: 'center',
    }
});